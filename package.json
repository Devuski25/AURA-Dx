{
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,green \"cd backend && .\\venv\\Scripts\\python -m uvicorn main:app --reload --port 8001\" \"cd frontend-new && npx vite --port 5174\"",
    "dev:backend": "cd backend && .\\venv\\Scripts\\python -m uvicorn main:app --reload --port 8001",
    "dev:frontend": "cd frontend-new && npx vite --port 5174"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "shadcn": "^4.16.0"
  }
}
