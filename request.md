here are the changes that i want:

- remove the add patient button in the patients page in the both the clinicin and admin account pages.
- the only users the super_admin see in the users box in the admin account page is the super_admin, i want all roles(super_admin, admin, clinician) to see all of the users no atter what the role is in the users box.
